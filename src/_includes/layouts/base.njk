<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ metaTitle ~ " - " if metaTitle }}{{ site.title }}</title>
  <meta name="description" content="{{ metaDescription if metaDescription else site.description }}">

  {# Open Graph + Twitter #}
  <meta name="twitter:card" content="summary">
  <meta name="twitter:creator" content="{{ site.authorTwitter }}">
  <meta property="og:url" content="{{ site.url }}">
  <meta property="og:title" content="{{ metaTitle ~ ' - ' if metaTitle }}{{ site.title }}">
  <meta property="og:description" content="{{ metaDescription if metaDescription else site.description }}">
  <meta property="og:image" content="{{ metaImage if metaImage else site.metaImage }}">

  {# Apple icon (minimal) #}
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">

  {# Preload fonts #}
  <link rel="preload" as="font" crossorigin type="font/woff2" href="/assets/fonts/opensans-regular-webfont.woff2">
  <link rel="preload" as="font" crossorigin type="font/woff2" href="/assets/fonts/opensans-italic-webfont.woff2">
  <link rel="preload" as="font" crossorigin type="font/woff2" href="/assets/fonts/opensans-semibold-webfont.woff2">

  {# JS (bundled ES6 - modules as cut the mustard) #}
  <script type="module" src="/assets/js/main.bundle.js?ref={{ site.buildDate | dateToFormat('X') }}"></script>

  {# CSS #}
  <link rel="stylesheet" href="/assets/css/main.css?ref={{ site.buildDate | dateToFormat('X') }}">
</head>
<body>
  {% include "partials/header.njk" %}
  {% block content %}{% endblock %}
  {% include "partials/footer.njk" %}
</body>
</html>
